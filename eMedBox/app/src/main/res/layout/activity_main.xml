<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/dataEntry"
        android:id="@+id/dataEntryButton"
        android:layout_marginTop="73dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/dataView"
        android:id="@+id/dataViewButton"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/dataEntryButton"
        android:layout_alignStart="@+id/dataEntryButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Medicine Update"
        android:id="@+id/dataUpdateButton"
        android:layout_centerVertical="true"
        android:layout_below="@+id/dataViewButton"
        android:layout_alignLeft="@+id/dataViewButton"
        android:layout_alignStart="@+id/dataViewButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="LED Control"
        android:id="@+id/ledButton"
        android:layout_centerVertical="true"
        android:layout_below="@+id/dataUpdateButton"
        android:layout_alignLeft="@+id/dataUpdateButton"
        android:layout_alignStart="@+id/dataUpdateButton" />


</RelativeLayout>
